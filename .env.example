# URL base da API (NestJS). Embutida no bundle em build time (NEXT_PUBLIC_*).
# Em build Docker é passada como --build-arg / via docker-compose.
NEXT_PUBLIC_API_BASE_URL=http://150.162.235.82:3000/api/v1

# Porta publicada pelo docker-compose no host (container sempre 3000).
PORT=3000
